@model UPTax.Model.ViewModels.VMLogin

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_AdminLogin.cshtml";
}

<!-- /.login-box -->

<div class="row">
    <div class="col-lg-12">
        <div class="text-center mb-5">
            @*<a href="@Url.Action("Index","Home")" class="logo"><img src="~/Content/assets/images/logo-main.png" height="24" alt="logo"></a>*@
            <h5 class="font-size-16 text-white-50 mb-4"><strong style="color: #F79725">ইউনিয়ন পরিষদ</strong><span style="color: #8BC63E"> ট্যাক্স</span> এডমিন ড্যাশবোর্ড</h5>
        </div>
    </div>
</div>
<!-- end row -->

<div class="row justify-content-center">
    <div class="col-xl-5 col-sm-8">
        <div class="card">
            <div class="card-body p-4">
                <div class="p-2">
                    <h5 class="mb-5 text-center">ভেতরে প্রবেশ করতে লগিন করুন</h5>
                    @using (Html.BeginForm("Index", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form", @autocomplete = "off" }))
                    {
                        @Html.AntiForgeryToken()
                        <div style="color:red; font-weight:bold">
                            @Html.Raw(Html.ValidationSummary(false).ToHtmlString())
                        </div>
                        <div class="row">
                            <div class="col-md-12">
                                <div class="form-group form-group-custom mb-4">
                                    @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", @pattern = "[A-Za-z0-9 _.-]*", @title = "User name must contain only characters, numbers, underscore, hyphen, dot.", @maxLength = "100", @required = "required", @id = "username" })
                                    @*@Html.ValidationMessageFor(model => model.UserName)*@
                                    @*<input type="text" class="form-control" id="username" required>*@
                                    <label for="username">ইউজার নেম</label>
                                </div>

                                <div class="form-group form-group-custom mb-4">
                                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", @required = "required", @maxLength = "100", @minLength = "6", @id = "userpassword" })
                                    @*@Html.ValidationMessageFor(model => model.Password)*@
                                    @*<input type="password" class="form-control" id="userpasswkord" required>*@
                                    <label for="userpassword">পাসওয়ার্ড</label>
                                </div>

                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="custom-control custom-checkbox">
                                            <input type="checkbox" class="custom-control-input" id="customControlInline">
                                            <label class="custom-control-label" for="customControlInline">মনে রাখুন</label>
                                        </div>
                                    </div>
                                </div>
                                <div class="mt-4">
                                    <button class="btn btn-success btn-block waves-effect waves-light" type="submit">লগ ইন</button>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

